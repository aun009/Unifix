#!/bin/bash
# Restart Cloud Instance

echo "Restarting cloud instance..."

# Example command to restart cloud instance (replace with actual cloud CLI command)
# For AWS: aws ec2 reboot-instances --instance-ids i-xxxxxxxxxxxx
# For GCP: gcloud compute instances reset INSTANCE_NAME
echo "Cloud instance restarted successfully!"

